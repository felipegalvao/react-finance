import expect from 'expect';
import React from 'react';
import ReactDOM from 'react-dom';
import $ from 'jquery';

import Item from 'Item';

describe('Item', () => {
  it('should exist', () => {
    expect(Item).toExist();
  })
})